{
  "name": "whatsapp-backend",
  "version": "1.0.0",
  "description": "Real-time Chat",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd whatsapp-frontend && npm install && npm run build",
    "start": "node server.js"
  },
  "author": "Eduardo Zaldivar",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.10",
    "pusher": "^4.0.0"
  }
}
